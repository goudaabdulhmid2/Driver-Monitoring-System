ultralytics
opencv-python-headless
flask
requests
Flask-Cors==5.0.0
numpy
# picamera2 is typically installed via apt on Raspberry Pi OS (python3-picamera2)
# but for our Docker environment, we rely on the system-level picamera2 if found,
# or standard OpenCV if using a USB webcam as requested.